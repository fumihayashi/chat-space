.chat-side
  .side-header
    .side-header__box
      %p.side-header__box__user-name
        = current_user.name
      %ul.side-header__box__menu
        %li<
        =link_to "#" do
          = icon('fas', 'edit')
        %li<
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')

  .groups
    =link_to "#" do
      .group
        %p.group__name groupname
        %p.group__latest-message まだメッセージがありません